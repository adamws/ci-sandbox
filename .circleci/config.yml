version: 2.1

jobs:
  build:
    docker:
      - image: cimg/node:19.7.0
    steps:
      - checkout
      - run:
          name: Build
          command: echo "test2"
  deploy-docs:
    docker:
      - image: cimg/node:19.7.0
    steps:
      - checkout
      - run:
          name: Build and deploy github pages
          working_directory: docs
          command: echo "test"

workflows:
  my-workflow:
    jobs:
      - build
      - deploy-docs
